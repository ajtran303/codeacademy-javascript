// `typeof` operator checks the value passed to it and returns a string of the data type

let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1;

console.log(typeof newVariable);
