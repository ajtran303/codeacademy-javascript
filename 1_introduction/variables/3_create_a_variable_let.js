// The `let` keyword means that a variable can be reassigned a different value.

// Variables can be declared without assigning a value -- the initialized value will be `undefined`.

let changeMe = true;

changeMe = false;

console.log(changeMe);
