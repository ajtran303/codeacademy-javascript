// `const` is short for constant. The value cannot be re-assigned, raises SyntaxError.

// `TypeError: Assignment to constant variable.`

// Initializing a const variable without a value raises

// `SyntaxError: Missing initializer in const declaration`

const entree = 'Enchiladas';

console.log(entree);

entree = 'Tacos';
