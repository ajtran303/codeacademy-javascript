// Use template literals to interpolate variables into strings.

// Note the backticks

const myName = 'AJ';

let myCity = 'Denver';

console.log(`My name is ${myName}. My favorite city is ${myCity}`);
