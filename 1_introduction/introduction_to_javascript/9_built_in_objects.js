// There are other built-in objects like console. They all have methods.

// These snippets use the Math and Number objects.

// Whole random number btwn 1 and 100
console.log(Math.floor(Math.random() * 100));

// Calculte smallest int greater than or equal to arg
console.log(Math.ceil(43.8));

//
console.log(Number.isInteger(2017));
