// The push method adds a new element to the end of an array. It mutates the array.

const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('study', 'pay bills');

console.log(chores);
