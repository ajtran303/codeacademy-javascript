// Arrays can be nested within each other to form more complex data structures.

// Bracket notation can be chained together to access sub-arrays' elements (and thus, update them).

let numberClusters = [[1, 2], [3, 4], [5, 6]]

const target = numberClusters[2][1] // 6
