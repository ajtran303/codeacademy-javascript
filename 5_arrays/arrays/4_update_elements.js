// Elements in an array can be updated by first accessing their index by bracket notation and then assigning a new value.

let groceryList = ['bread', 'tomatoes', 'milk'];

groceryList[1] = 'avocados';
