// if...else statements allow for binary decisions

let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
} else { // execute if false
  console.log('Time to wait for a sale.');
}
