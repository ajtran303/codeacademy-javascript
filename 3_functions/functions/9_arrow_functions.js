// ES6 arrow function syntax is a concise way to write functions

// const plantNeedsWater = function(day) {
//   if (day === 'Wednesday') {
//     return true;
//   } else {
//     return false;
//   }
// };

const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  };
};
