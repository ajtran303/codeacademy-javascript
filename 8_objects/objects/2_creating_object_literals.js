// objects are collections of key-value pairs and the literal notation uses curly braces

// An object literal with two key-value pairs
let spaceship = {
  'Fuel Type': 'diesel',
  color: 'silver'
};

// Write your fasterShip object literal below

let fasterShip = {
  'Fuel Type': 'Turbo Fuel',
  color: 'silver'
};
