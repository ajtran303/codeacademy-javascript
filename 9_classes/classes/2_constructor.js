// Every class in JS has a constructor method that is called when a new object is instantiated

// It initializes new object instances with their own properties and methods

class Surgeon {
  constructor(name, department){
    this.name = name;
    this.department = department;
  }
}
